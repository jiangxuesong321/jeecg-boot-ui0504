<template>
  <j-modal
    :title="title"
    :width="width"
    :visible="visible"
    switchFullscreen
    @ok="handleOk"
    :okButtonProps="{ class:{'jee-hidden': disableSubmit} }"
    @cancel="handleCancel"
    cancelText="关闭">
    <bas-supplier-assessment-form ref="realForm" @ok="submitCallback" :disabled="disableSubmit"></bas-supplier-assessment-form>
  </j-modal>
</template>

<script>

  import BasSupplierAssessmentForm  from './BasSupplierAssessmentForm'
  export default {
    name: 'AttachmentManagementModal',
    components: {
      BasSupplierAssessmentForm
    },
    data () {
      return {
        title:'',
        width:800,
        visible: false,
        disableSubmit: false
      }
    },
    methods: {
      // setData(record){
      //   this.modelDefault = JSON.parse(JSON.stringify(record));
      // },
      add () {
        this.visible=true
        this.$nextTick(()=>{
          this.$refs.realForm.add();
        })
      },
      edit (record) {
        this.visible=true
        this.$nextTick(()=>{
          this.$refs.realForm.edit(record);
        })
      },
      close () {
        this.$emit('close');
        this.visible = false;
      },
      handleOk () {
        this.$refs.realForm.submitForm();
      },
      submitCallback(){
        this.$emit('ok');
        this.visible = false;
      },
      handleCancel () {
        this.close()
      }
    }
  }
</script>